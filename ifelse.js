/**
 * o algoritimo ifelse serve para validação de dados, por exemplo:
 * if (variavel == variavel2) {algo ira acontecer se a variavel for igual a avariavel2}  else {algo ira acontecer se a avariavel for diferente da vairiavel2}
 * 
 * if: nome do algoritimo
 * parênteses (): local onde se declaram os parâmetros ou expressôes a ser validada
 * 
 * chaves {}:bloco de códigos a serem executados, ou seja, tudo que estiver dentro do bloca de código será executado
 * 
 * else: "senão" é onde serão executados os código, que estiverem dentro do bloco {}, caso a validação do algoritimo "if" retorne falsa
 * 
 * operadores lógicos:
 * 
 * == serve para comparar se uma variavel/valor é igual a outra 
 * != serve para comparar se uma variavel/valor é diferente  a outra 
 * > serve para comparar se uma variavel/valor é maior a outra 
 * < serve para comparar se uma variavel/valor é menor a outra 
 * >= serve para comparar se uma variavel/valor é maior ou igual a outra 
 * <= serve para comparar se uma variavel/valor é menor ou igual a outra 
 * 
 *
 * se "falso": adicionar o ponto de exclamação no inicio da declaração de parâmetros/expressôes, por exemplo:
 * ir (!variavel == variavel2) { bloco de código } aqui significa que: 
 * se a comparação da variavel1 com a variavel2 retornar falso, 
 * os códigos que estiverem dentro do bloco de códigos serão executados, não havendo a necessidade do uso da clausula "else"
 * 
 * NOW
 * 
 * Vamos criar dois input, onde cada input irá para cada uma  das duas variaveis, ou seja:
 * 
 * <input type1="text" id="txtVar1" />
 * <input type2="text" id="txtVar2" />
 * 
 * 
 * <input type1 = document.getElementByID("txtVar1" />);
 * <input type2 = document.getElementByID("txtVar2" />);
 * 
 * txtVar1. value que tem que ser comparado com o txtVar2.value
 * 
 * enviar os dadosvia console.log()
 */

